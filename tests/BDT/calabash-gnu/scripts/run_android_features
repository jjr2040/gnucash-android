cd ..

PARAMS="-p android"

while getopts ":rd:" OPTION; do
        case $OPTION in

                r)
                    PARAMS=$PARAMS" --format json -o cucumber.json"
                    ;;
                d)
                    PARAMS=$PARAMS" ADB_DEVICE_ARG=$OPTARG"

                    ;;
                [?])    echo "Usage: $0 [-r] [-d DEVICE_ID].\n     -r: should create reports.\n     DEVICE_ID: where to run tests."
                        exit 1;;
        esac
done

# clear old files
rm -rf screenshot*
rm -rf test_servers
# rm ../../../app/build/outputs/apk/development/debug/GnucashAndroid.apk

# change apk name
# mv ../../../app/build/outputs/apk/development/debug/*.apk ../../../app/build/outputs/apk/development/debug/GnucashAndroid.apk

# resign apk
bundle exec calabash-android resign "../../../app/build/outputs/apk/development/debug/GnucashAndroid.apk"
bundle exec calabash-android build "../../../app/build/outputs/apk/development/debug/GnucashAndroid.apk"
# run tests
bundle exec calabash-android run "../../../app/build/outputs/apk/development/debug/GnucashAndroid.apk" $PARAMS